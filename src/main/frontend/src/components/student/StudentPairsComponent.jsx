import React, { Component } from 'react';
import withRouter from '../Router';

class StudentPairsComponent extends Component {

    render() {
        return (
            <div>
                Student pairs component.
            </div>
        );
    }
}

export default withRouter(StudentPairsComponent);